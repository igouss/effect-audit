# Fixture: a workspace with no `role = "domain"` crate, for --require-domain.
[workspace]

[package]
name = "nodomain-adapter"
version = "0.0.0"
edition = "2021"

[package.metadata.hex-arch]
role = "driven-adapter"
